name: web-9router
version: 1.0.0
description: "Route web_search and web_extract through a self-hosted 9router gateway."
author: dipandhali2021
kind: backend
provides_web_providers:
  - 9router
requires_env:
  - NINEROUTER_API_KEY
